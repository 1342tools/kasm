/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{html,js,svelte,ts}'],
  darkMode: 'selector', // Use the 'selector' strategy based on [data-theme="dark"]
  theme: {
    extend: {},
  },
  plugins: [],
}
